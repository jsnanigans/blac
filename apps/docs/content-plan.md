# Documentation Content Plan

Focus areas:

- Core concepts: `StateContainer`, `Cubit`
- Registry lifecycle and instance management
- Decorator configuration (`isolated`, `keepAlive`, `excludeFromDevTools`)
- Plugins and `PluginContext`
- Watch and tracking utilities
- React integration with `useBloc`
