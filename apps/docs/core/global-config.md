# Global Configuration

`@blac/core` does not expose a global configuration API in the current release.

Use the `@blac` decorator for class-level configuration and the registry helpers for instance management.
