{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [
        "dist/**"
      ]
    },
    "build:demo": {
      "outputs": [
        "site/**"
      ]
    },
    "lint": {}
  }
}

