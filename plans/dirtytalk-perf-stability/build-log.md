# Build log — dirtytalk-perf-stability

Deferred/adjacent items noticed during the build. Review with `/review-plan-status`.

- [phase 1] P4b `ancestorIds` memo was unsound (staleness → dropped marks) as first written by Unit S — `path-interner.ts:97-108` memoized per-id but `intern()` grows `_map` monotonically without invalidation. **FIXED in-scope this run** (not deferred): `intern()` now clears the `_ancestorIds` memo on each new path (`path-interner.ts:49`), + regression test in `path-interner.test.ts`. Recorded here for traceability — surfaced by the adversarial verify (`holds:false`) before commit.
- [phase 1] internAncestor now unconditionally interns the real path — `path-interner.ts:60-64` — verified idempotent (no size drift) at all structural call sites, but `blac-react`'s `expandWithAncestors` (useBloc.ts ~:783-806) calls `internAncestor` on ancestor strings not guaranteed pre-interned; a real-but-harmless growth-behavior change with no test asserting prior idempotency there. Low risk; suggested fix: add a targeted test in blac-react if this path is ever measured for interner growth.
- [phase 2] E3 `dispose()` called synchronously from inside a subscriber mid-`#flush()` does not halt the in-flight pass — `dirty-channel.ts:88,104,172` — `#disposed` is checked once at flush entry; already-snapshotted `live` entries still fire. No future-flush resurrection (accumulated reset → no reschedule). Out of X2's done-check scope. Suggested fix if hardened later: check `entry.alive`/`#disposed` inside the flush loop.
- [phase 2] `scheduler.cancel()` is now first exercised in production by `dispose()` — `dirty-channel.ts:166` — Micro/RAF `cancel()` clear the scheduler's ENTIRE pending set, not just this channel's `#boundFlush`. If one scheduler instance is ever shared across multiple channels/containers, disposing one cancels another's pending flush. Latent hazard activated (not caused) by dispose(); nothing today shares a scheduler. Suggested fix if sharing is introduced: per-channel cancel token.
- [validation] Pre-existing repo `format:check` failure on `packages/blac-core/reports/verify-depend-identity.md` and `verify-dispose-eventbus.md` — committed in `3ebd5a9d "review comments"`, entirely outside this change's diff. NOT fixed (out of scope, unrelated files). Repo owner should run `pnpm format` on those report files independently.
- [validation] Pre-existing lint warning `dirtytalk-spatial/src/container.test.ts:665:18` (non-null assertion) — unrelated to this change, 0 errors.
- [artifact] Investigator left `reports/dirtytalk-structural-phase1-phase3-verify.md` (untracked) — verify byproduct, not committed with the code units.
