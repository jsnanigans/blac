# Build log — dirtytalk-fixes-cleanups

Deferred/noted items surfaced during the build. Review with `/review-plan-status`.

- [phase 1] `ownKeys` trap reuses `pinArrayPath()` for plain objects too, but that helper keeps its array-specific name + comment framing — `packages/dirtytalk-structural/src/tracker.ts:150` — naming/doc nit only (pin/intern logic is identical for objects & arrays); verify:A confirmed functionally correct. Suggested fix: rename to `pinOwnEntryPath` or generalize the comment.
- [phase 1] Structural `integration.test.ts` docstring still says "Imports exclusively from the package barrels" but now imports `pathsFromPatch` from `./diff` after the de-barrel — `packages/dirtytalk-structural/src/integration.test.ts:5` — stale comment; left as-is to avoid churn. Suggested fix: reword the docstring.
- [phase 3] blac-react tests/comments still document the old "single consumer always marks ALL_PATHS" behavior that P1 removed (`useBloc.auto-track-optimization.test.tsx:24-29,261-263`; `useBloc.messenger-reproduction.test.tsx:346-350`; `useBloc.track-lifecycle.test.tsx:336-341`; `useBloc.getter-tracking.test.tsx:74-77`) — verify:B confirmed **no assertions break** (all 172 blac-react tests pass), but the comments now misdescribe behavior. Suggested fix: update the comments in a follow-up.
- [phase 2/plan gap] The plan modeled B→C as a **source-level** dependency, but `@dirtytalk/structural` consumes `@dirtytalk/engine` via its built `dist/` (workspace:^, `types: ./dist/index.d.ts`). C's new 3-arg `DirtyChannel` ctor was invisible to B until `@dirtytalk/engine` was rebuilt — caused the first validation's structural tsc error + onError runtime test failure. Resolved by `pnpm --filter @dirtytalk/engine build`. Suggested fix: add an engine build step ahead of structural validation in future cross-package plans.
- [pre-existing, out of scope] `pnpm format:check` flags `reports/verify-depend-identity.md` and `reports/verify-dispose-eventbus.md` — pre-existing formatting debt (committed in ac7a03ab, never touched by this work). Not fixed here to stay scope-locked.
