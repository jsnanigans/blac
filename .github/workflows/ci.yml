name: CI

on:
  pull_request:

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: voidzero-dev/setup-vp@v1
        with:
          node-version: lts
          cache: true
      - run: vp lint

  format:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: voidzero-dev/setup-vp@v1
        with:
          node-version: lts
          cache: true
      - run: vp format:check

  typecheck:
    name: Typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: voidzero-dev/setup-vp@v1
        with:
          node-version: lts
          cache: true
      - run: vp typecheck

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: voidzero-dev/setup-vp@v1
        with:
          node-version: lts
          cache: true
      - run: vp build

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: voidzero-dev/setup-vp@v1
        with:
          node-version: lts
          cache: true
      - run: vp test
